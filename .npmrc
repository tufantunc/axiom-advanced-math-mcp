# The lockfile is tracked in git, so npm must keep it current — otherwise it
# drifts silently and `npm ci` refuses to install at all. (It had drifted six
# months, to the point where npm ci failed and would have installed a
# vulnerable mathjs.)
save-exact=true
