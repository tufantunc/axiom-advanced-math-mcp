# S5976 (round 2) — the five surviving clusters

Date: 2026-09-03
Branch: `fix/sonar-s5976b` (from `main` 7afb945)

## Clusters

1. integration.test.ts:109 — calculus remainder (definite integral, limit,
   taylor, solve ODE): one shape, problem → needle.
2. integration.test.ts:151 — algebra (factor, simplify, expand, partial
   fractions): same shape, one or two needles.
3. integration.test.ts:313 — verify tool over MCP (TRUE, TRUE, FALSE): one
   shape, differing verdicts → expected-verdict column.
4. verify.test.ts:57 — solution cluster (x=2, x=-2, reject x=3, x=1): same.
5. verify.test.ts:125 — two JSON evaluated:false rows parametrize; the
   UNKNOWN text-mode sibling is heterogeneous and stays its own it.

## House hardening carried in

- New verdict tables match the full `Verified: TRUE` / `Verified: FALSE`
  line (last round's mutation evidence: every FALSE output also contains
  the letter T).
- Integration needles attach to probed real fragments where deterministic;
  needles that stay loose carry a comment saying why.

## Invariants

Rows preserve each test's assertions byte-identically except the two
documented strengthenings above; the row count equals the replaced tests
(cluster 5 splits 2+1, no loss); five gates; review-pro sequential.
